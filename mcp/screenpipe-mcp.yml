generated: '2026-07-21'
method: searched
status: published
source: https://docs.screenpipe.com/mcp-server
server:
  name: screenpipe
  package: screenpipe-mcp
  transport: stdio
  install: npx -y screenpipe-mcp
  http_variant:
    package: screenpipe-mcp-http
    transport: http
    install: npx -y screenpipe-mcp-http --port 3031
  backing_api: openapi/screenpipe-openapi-original.yml
  requires: The screenpipe desktop app / recorder must be running locally (REST API on http://localhost:3030);
    the MCP server proxies to that local API.
clients_documented:
- claude-code
- claude-desktop
- codex
- cursor
- cline
- continue
- gemini-cli
- copilot-cli
- msty
- opencode
tools:
- name: search-content
  description: Query recorded screen content, audio transcriptions, and input events with filters for
    time range, application, window title, content type, and speaker.
  source_operation: openapi/screenpipe-openapi-original.yml#routes_search_search
- name: export-video
  description: Generate a video export from screen recordings for a specified time range with configurable
    frame rate.
setup_via_cli: npx -y screenpipe@latest agent setup <target>
setup_targets:
- openclaw
- hermes
- claude-code
- claude-desktop
- codex
- cursor
- windsurf
deployment:
  mode: none
  endpoint: http://localhost:3030
  verified: probed
  probe: dead
  note: the endpoint this manifest claimed did not answer; recorded as none rather than deleted so the
    claim stays auditable
  checked: '2026-08-12'
  source: catalog MCP census
